<?php

namespace App\Models;

/**
 * Identical to Calculator but handles multiple trips based on search results
 */
class TripCalculator {
  /**
   * @param Array $query $_GET previously validated
   * @param Array $results Array of searchResults
   * @return Array App\Models\Price array
   */
  public function calculatePrices($query, $results) {
    
  }
}